<p>Welcome to the board!</p>
<table id='board' class='table'>
  <!--<thead>
		<tr>
		<td></td>
		<td>
		Search users by technologies they know: <input type='text' ng-model='search.skillsTO'>
		</td>
		<td>
		Search users by technologies they need help with: <input type='text' ng-model='search.skillsTL'>
		</td>
		<td></td>
		<td></td>
		<tr>
	</thead>-->
	<thead affix affix-options="{placeholder: true}" id='boardHead'>
		<tr>
			<th style='width: 8%'></th>
			<th style='width: 8%'>User ID</th>
			<th style='width: 25%'h>Search users by technologies they know: <input type='text' ng-model='search.skillsTO'><br>Technologies user knows</th>
			<th style='width: 25%'>Search users by technologies they need help with: <input type='text' ng-model='search.skillsTL'><br>Technologies user needs help</th>
			<th style='width: 25%'>User message</th>
			<th style='width: 9%'>Contact User</th>
		</tr>
	</thead>
	<tbody>
	<tr ng-repeat='user in users | filter: search:strict'>
		<td><img alt='Image not found'></td>
		<td>{{user.username}}</td>
		<td><span class='boardSkills' ng-repeat='skill in user.skillsTO track by $index'>{{skill}}</span></td>
		<td><span class='boardSkills' ng-repeat='skill in user.skillsTL track by $index'>{{skill}}</span></td>
		<td>{{user.bio}}</td>
		<td><a ng-click='open(user.email)'><span class='glyphicon glyphicon-envelope' id='contactGlyph' ng-click='modalEmail(user.email)'></span></a></td>
	</tr>
	</tbody>
</table>

<script type='text/ng-template' id='loggedInModal.html'>
        <div class='modal-header'>
            <h3 class='modal-title'>Contact User</h3>
        </div>
        <div class='modal-body'>
	  <p>User email: {{userEmail}}</p>
        </div>
        <div class='modal-footer'>
            <button class='btn' type='button' ng-click='close()'>Close</button>
        </div>
    </script>


<script type='text/ng-template' id='notLoggedInModal.html'>
        <div class='modal-header'>
            <h3 class='modal-title'>Contact User</h3>
        </div>
        <div class='modal-body'>
	  <p>Need to be logged in to contact user</p>
	  <span><a href='#/login' ng-click='close()'>Log in</a></span>
	  <span><a href='#/signUp' ng-click='close()'>Sign up</a></span>
        </div>
        <div class='modal-footer'>
            <button class='btn' type='button' ng-click='close()'>Close</button>
        </div>
    </script>
